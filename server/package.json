{
  "name": "chat-in",
  "version": "1.0.0",
  "scripts": {
    "start-dev": "nodemon",
    "reset": "rimraf ./node_modules && rm yarn.lock && yarn",
    "build": "rimraf ./build && tsc"
  },
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.11.9",
    "nodemon": "^2.0.15",
    "rimraf": "^3.0.2",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.2"
  }
}
